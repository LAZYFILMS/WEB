Product catalogue images for LAZY 500 (product/box/roll shots only, not sample photos).

Put product images here, named:
  1.png   - main product image (also used as the big image on load)
  2.png
  3.png
  (optional: 4.png, 5.png — add more in lazy500.html if you add files)

Use product photos only (e.g. box, roll, different angles). The strip under the main image will show these; clicking one updates the large image.
